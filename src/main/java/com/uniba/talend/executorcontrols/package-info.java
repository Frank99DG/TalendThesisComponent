// this tells the framework in which family (group of components) and categories (UI grouping)
// the components in the nested packages belong to
@Components(family = "ExecutorControls", categories = "Misc")
@Icon(value = CUSTOM, custom = "ExecutorControls") // icon is located at src/main/resources/icons/ExecutorControls.svg
package com.uniba.talend.executorcontrols;

import static org.talend.sdk.component.api.component.Icon.IconType.CUSTOM;

import org.talend.sdk.component.api.component.Components;
import org.talend.sdk.component.api.component.Icon;
